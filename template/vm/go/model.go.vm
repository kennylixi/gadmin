// ============================================================================
// This is auto-generated by gen tool only once. Fill this file as you wish.
// ${.functionName}Model数据层
// @author ${.author}
// @date ${.datetime}
// ============================================================================

package model

import (
    "${.packageName}/internal/modules/${.moduleName}/model/entity"
)

// Fill with you ideas below.

${- if .isSub}
// ${.functionName}扩展实体
type ${.ClassName}Ex struct {
    ${.ClassName}
}
${- end}

// ${.ClassName}ListInput 查询列表入参
type ${.ClassName}ListInput struct {
${- range $index, $column := .columns}
${- if eq $column.IsQuery "1"}
${- $comment:=$column.ColumnComment | comment}
    ${$column.GoField} ${$column.GoType} `json:"${$column.GoField|uncapitalize}"`// ${$comment}
${- end}
${- end}
}
// ${.ClassName}ListOutput 查询列表出参
type ${.ClassName}ListOutput struct {
    List  []*entity.${.ClassName}   `json:"list"`  // 列表
    Total int                 `json:"total"` // 数据总数
}

// ${.ClassName}AddInput 新增入参
type ${.ClassName}AddInput struct {
${- range $index, $column := .columns}
${- if or (eq $column.IsInsert "1") (eq $column.GoField "CreateBy")}
${- $comment:=$column.ColumnComment | comment}
    ${$column.GoField} ${$column.GoType} `json:"${$column.GoField|uncapitalize}"`// ${$comment}
${- end}
${- end}
}

// ${.ClassName}EditInput 修改入参
type ${.ClassName}EditInput struct {
${- range $index, $column := .columns}
${- if or (eq $column.IsEdit "1") (eq $column.IsPk "1") (eq $column.GoField "UpdateBy")}
${- $comment:=$column.ColumnComment | comment}
    ${$column.GoField} ${$column.GoType} `json:"${$column.GoField|uncapitalize}"`// ${$comment}
${- end}
${- end}
}